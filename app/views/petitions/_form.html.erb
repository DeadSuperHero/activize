<%= simple_form_for(@petition) do |f| %>
  <% if @petition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@petition.errors.count, "error") %> prohibited this petition from being saved:</h2>

      <ul>
      <% @petition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :cover, label: "Cover Image" %>
  </div>
  <div class="field">
    <%= f.input :title, label: "Title" %>
  </div>
  <div class="field">
    <%= f.input :description %>
  </div>
  <div class="field">
    <%= f.input :signum, label: "Minimum amount of signatures?" %>
  </div>
  <div class="field">
    <%= f.input :target, label: "Who is this petition for?" %>
  </div>
  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
